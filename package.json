{
  "name": "playg",
  "version": "0.1.0",
  "private": true,
  "license": "none",
  "scripts": {
    "build": "truffle compile --all && yarn gen:contract-typings && tsc -b",
    "clean": "rm -rf build && yarn clean:networks",
    "clean:networks": "rm -f networks/127001.json",
    "migrate": "yarn clean:networks && truffle migrate --network ganache --reset",
    "test": "yarn clean:networks && tsc -b && truffle test --network ganache build/scripts/**/*.spec.js",
    "gen:contract-typings": "typechain --target ethers --outDir typings/contracts './build/contracts/*.json'",
    "lint": "yarn lint:ts && yarn lint:sol",
    "lint:fix": "yarn lint:ts:fix && yarn lint:sol:fix",
    "lint:sol": "solium -d .",
    "lint:sol:fix": "solium -d contracts/ --fix",
    "lint:ts": "tslint -c tslint.json -p .",
    "lint:ts:fix": "tslint -c tslint.json -p . --fix",
    "ganache": "ganache-cli --networkId ${npm_package_config_ganacheNetworkID} --host 0.0.0.0 --port ${npm_package_config_ganachePort} --gasLimit ${npm_package_config_ganacheGasLimit} --gasPrice ${npm_package_config_ganacheGasPrice} --defaultBalanceEther ${npm_package_config_etherBalance} --deterministic --mnemonic \"${npm_package_config_mnemonic}\" ${npm_package_config_extra}",
    "ganache:verbose": "ganache-cli --networkId ${npm_package_config_ganacheNetworkID} --verbose --host 0.0.0.0 --port ${npm_package_config_ganachePort} --gasLimit ${npm_package_config_ganacheGasLimit} --gasPrice ${npm_package_config_ganacheGasPrice} --defaultBalanceEther ${npm_package_config_etherBalance} --deterministic --mnemonic \"${npm_package_config_mnemonic}\" ${npm_package_config_extra}",
    "ganache:stop": "ps aux | grep ganache-cli | grep -v grep | awk '{print $2}' | xargs kill -9",
    "deploy:ganache": "yarn migrate",
    "deploy:rinkeby": "truffle migrate --network rinkeby"
  },
  "config": {
    "mnemonic": "dose weasel clever culture letter volume endorse used harvest ripple circle install",
    "ganacheNetworkID": 127001,
    "ganachePort": 8545,
    "ganacheGasLimit": "0xfffffffffff",
    "ganacheGasPrice": "0x01",
    "etherBalance": "100000",
    "extra": ""
  },
  "dependencies": {
    "ethers": "^4.0.27"
  },
  "devDependencies": {
    "@types/chai-as-promised": "^7.1.0",
    "@types/chai-string": "^1.4.1",
    "@types/mocha": "^5.2.6",
    "chai": "^4.2.0",
    "chai-as-promised": "^7.1.1",
    "chai-bignumber": "^3.0.0",
    "chai-string": "^1.5.0",
    "dotenv": "^7.0.0",
    "eth-gas-reporter": "^0.1.12",
    "ganache-cli": "6.4.1",
    "openzeppelin-solidity": "2.2.0",
    "solc": "0.5.6",
    "solium": "^1.2.3",
    "truffle": "^5.0.8",
    "truffle-contract": "^4.0.9",
    "truffle-deploy-registry": "^0.5.1",
    "truffle-hdwallet-provider": "1.0.5",
    "tslint": "^5.14.0",
    "typechain": "^0.3.13",
    "typescript": "3.3.4000",
    "zos-lib": "^2.2.2"
  }
}
